import {View, Text, Button} from 'react-native';
import {componentsStyles} from '../components.styles.js';

export default function Abonnements({navigation}) {
  return (
    <View>
      <Text style={componentsStyles.h1}> Mes abonnements </Text>
    </View>
  );
}
