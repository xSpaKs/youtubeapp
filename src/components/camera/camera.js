import {View, Text, Button} from 'react-native';
import {componentsStyles} from '../components.styles.js';

export default function Camera({navigation}) {
  return (
    <View>
      <Text style={componentsStyles.h1}>Caméra</Text>
    </View>
  );
}
